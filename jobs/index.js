/**
 * ===========================================
 * JOBS INDEX DOSYASI
 * ===========================================
 */

const cronManager = require('./cronManager');

module.exports = {
    cronManager
};
